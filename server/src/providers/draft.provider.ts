// import { DataSource } from 'typeorm';
// import { Draft } from '../entities/draft.entity';

// export const draftProvider = {
//   provide: 'DraftProvider',
//   useFactory: (dataSource: DataSource) => dataSource.getRepository(Draft),
// };
